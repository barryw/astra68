diff --git a/src/ext4_hash.c b/src/ext4_hash.c
index ff6d031..b7f0436 100644
--- a/src/ext4_hash.c
+++ b/src/ext4_hash.c
@@ -266,8 +266,13 @@ int ext2_htree_hash(const char *name, int len, const uint32_t *hash_seed,
 	hash[2] = 0x98BADCFE;
 	hash[3] = 0x10325476;
 
-	if (hash_seed)
-		memcpy(hash, hash_seed, sizeof(hash));
+	if (hash_seed) {
+		/* s_hash_seed is an on-disk little-endian array. */
+		hash[0] = to_le32(hash_seed[0]);
+		hash[1] = to_le32(hash_seed[1]);
+		hash[2] = to_le32(hash_seed[2]);
+		hash[3] = to_le32(hash_seed[3]);
+	}
 
 	switch (hash_version) {
 	case EXT2_HTREE_TEA_UNSIGNED:
